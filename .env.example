# Do not commit the real token. Put these values in the server-side .env file.
NEWAPI_ACCESS_TOKEN=
NEWAPI_USER_ID=
CHECK_INTERVAL_SECONDS=60
CHECK_CONCURRENCY=1
TEST_TIMEOUT_SECONDS=60
SUCCESS_COUNT=1
# Empty means all groups; otherwise comma-separated exact group names.
RECOVERY_GROUPS=default
